benchmark:
  name: MMStar
  task_type: multiple_choice
  official_split: offline_eval
  expected_examples: 1500
  primary_metrics: [accuracy, visual_gain]

data:
  manifest_train: data/processed/mmstar/train.jsonl
  manifest_eval: data/processed/mmstar/eval.jsonl
  image_root: data/raw/mmstar/images

model:
  thought_slots: 8
  num_classes: 4

retrieval:
  enabled: false

reporting:
  table: "4.2"
  claim: dynamic_visual_grounding
